name: Build iOS IPA (macOS Runner)

on:
  workflow_dispatch:
    inputs:
      server_url:
        description: 'Server URL (e.g. https://dipsin.com)'
        required: true
        default: 'https://dipsin.com'

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - name: Recreate iOS platform (force Podfile generation)
        run: |
          rm -rf ios
          flutter create --platforms=ios .
      - run: flutter precache --ios
      - name: Check ios directory
        run: ls -la ios/ || echo 'ios dir missing'
      - name: Pod install
        run: |
          cd ios
          pod install --repo-update || echo 'pod install skipped'
      - run: |
          flutter build ios --release --no-codesign \
            --dart-define=SERVER_URL=${{ inputs.server_url }}
      - run: |
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r neo-ant.ipa Payload/
      - uses: actions/upload-artifact@v4
        with:
          name: neo-ant-ios-release
          path: neo-ant.ipa
          retention-days: 7
